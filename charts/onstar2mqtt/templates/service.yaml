apiVersion: v1
kind: Service
metadata:
  name: {{ include "onstar2mqtt.fullname" . }}
  labels:
    {{- include "onstar2mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onstar2mqtt.selectorLabels" . | nindent 4 }}
