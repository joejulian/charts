apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: {{ include "hostpath-pv-remediator.leaderElectionLeaseName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hostpath-pv-remediator.labels" . | nindent 4 }}
spec: {}
---
apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: {{ include "hostpath-pv-remediator.repairLeaseName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hostpath-pv-remediator.labels" . | nindent 4 }}
spec:
  leaseDurationSeconds: {{ .Values.remediation.leaseDurationSeconds }}
