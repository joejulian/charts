{{- if .Values.settings.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "transmission.fullname" . }}
  labels:
    {{- include "transmission.labels" . | nindent 4 }}
data:
  settings.json: |
{{ .Values.settings.json | indent 4 }}
{{- end }}
