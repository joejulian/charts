apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ include "external-dns.fullname" . }}-test"
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: "{{ include "external-dns.fullname" . }}-test"
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "{{ include "external-dns.fullname" . }}-test"
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
subjects:
  - kind: ServiceAccount
    name: "{{ include "external-dns.fullname" . }}-test"
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: "{{ include "external-dns.fullname" . }}-test"
---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "external-dns.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "external-dns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  serviceAccountName: "{{ include "external-dns.fullname" . }}-test"
  containers:
    - name: kubectl
      image: rancher/kubectl:v1.32.2
      command: ["kubectl"]
      args:
        - rollout
        - status
        - deployment/{{ include "external-dns.fullname" . }}
        - -n
        - {{ .Release.Namespace }}
        - --timeout=180s
  restartPolicy: Never
